import { FiltradoProductosPipe } from './filtrado-productos-pipe';

describe('FiltradoProductosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltradoProductosPipe();
    expect(pipe).toBeTruthy();
  });
});
